# 最新更新

版本：v171.5.1

## 返回 Tracker 按鍵

- 「回到 Tracker」改為與主頁標題列按鍵完全相同的白底、藍字、淡色邊框樣式。
- 主頁與學習總結現在共用同一份按鍵樣式，之後修改時不會再次出現外觀落差。
- 滑鼠指向、鍵盤焦點與手機版伸展方式皆保持一致。

## 學習總結版面

- 「與上期比較」已整合進本週／本月小結，不再占用獨立卡片。
- 平均起床時間移至小結右側，並在平均時間下方顯示「相較上週／上月」。
- 起床時間差以粗體顯示「早起／晚起 X 小時 X 分鐘」；資料不足與時間相同時使用對應狀態文字。
- 科目分配取得較大的頁面欄位，學習趨勢保留在右側。
- 科目明細在桌面每列顯示四項，超過四項自動換列；窄螢幕自動改成兩項一列。

## 科目圓環

- CNN 互動英文與常春藤在英文科目明細中統一合併為「雜誌」。
- 兩者原始紀錄不會被改名或刪除，只合併學習總結中的顯示與時間加總。
- 點擊明細小圓環返回全部科目時，圓環會平順放大並移回中央，不再瞬間跳回。

## 狀態配色

- 本期小結判定為「持平」時使用淺藍底。
- 增加維持淺綠底，下降維持淺紅底。

更新資料夾：`gsat-study-tracker-v171.5.1-shared-header-button`

## 驗證

- 331 項單元／回歸測試通過。
- TypeScript 型別檢查、Vite 正式建置與真實瀏覽器 E2E 通過。

本次不需要新增 Supabase migration，也不需要重新部署 Edge Function；只需部署新版 GitHub Pages 前端。

## Commit 建議

`style(summary): match Tracker header action button`
