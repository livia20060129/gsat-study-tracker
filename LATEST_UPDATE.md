# 最新更新

版本：v171.6.18

## 作息時間與睡眠統計

- 首頁「起床時間」改為固定高度的「作息時間」，使用唯一一組「起床／就寢」分段滑塊，共用原有小時與分鐘輸入介面。
- 起床與就寢各自保留輸入草稿；滑塊切換前會先同步目前畫面狀態，正式寫入儲存空間仍在欄位失焦時發生。切換不會重建欄位或改變手機、桌面版高度。
- 原有 `wakeTime`、`wakeHour` 與 `wakeMinute` 行為完整保留。新增就寢資料包含 `time`、`dateTime`、`nextDay`，不只保存缺少日期的時間字串。
- 當日 00:00～05:59 的就寢會解析為隔日凌晨，介面同步顯示「此時間視為隔日凌晨」；06:00 起維持當日。

## 睡眠計算

- 睡眠時間歸入起床日，使用前一日就寢的完整日期時間減去當日起床日期時間。
- 只有結果大於 0 且不超過 18 小時才納入平均；缺少任一資料顯示「資料不完整」，日期時間不合理則顯示「請確認時間」，原始輸入都會保留。
- 平均就寢時間以跨午夜延伸分鐘計算，因此 23:50 與 00:10 的平均會顯示 00:00，而不是 12:00。
- 與上期相比固定顯示「平均多睡／少睡 X 小時 Y 分鐘」；差異小於 5 分鐘顯示「大致持平」。不產生健康診斷或自由生成評語。

## 學習總結

- 在本期小結下方新增獨立「個人狀態」，顯示平均睡眠、平均就寢、平均起床、有效紀錄數、每日睡眠時間趨勢及上期比較。
- 個人狀態沿用學習總結唯一的週／月滑塊。週模式讀取目前週，月模式讀取目前月；兩者都會額外讀取區間第一天前一日的就寢紀錄。
- 外出、身體不適與較疲累日仍正常納入有效睡眠統計。
- 睡眠資料不參與學習時間、完成率、科目分配或本期學習小結。

## 資料相容與安全

- StudyRecord schema 升級為 v2；舊 schema 0／1 會保留原有起床紀錄並安全升級，缺少就寢時間即視為尚未填寫。
- LocalStorage、JSON 匯入／匯出、Supabase JSON payload、Cloud round-trip、三方合併與衝突提示都保留作息資料。
- `study_records.payload` 原本就是 JSON payload，因此不需要新增 Supabase 欄位或資料庫 migration；重新建置前端即可使用。

## 驗證

- 完整單元／回歸測試、TypeScript 型別檢查及 Vite 正式建置通過。
- 11 項 Chromium 真實瀏覽器 E2E 通過，涵蓋舊起床資料、草稿切換、失焦儲存、凌晨提示、手機固定高度、學習總結與既有頁面 regression。

更新資料夾：

- `gsat-study-tracker-v171.6.18-routine-sleep-required-files`
- `gsat-study-tracker-v171.6.18-routine-sleep-full-project`

## Commit 建議

`feat(routine): add bedtime records and sleep statistics`
