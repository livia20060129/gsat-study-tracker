# 最新更新

版本：v171.0.83

## 本次修正

- 化學頁碼已改成 109–114，章節卻停在先前輸入的範圍；勾選「訂正」也沒有出現錯因欄。
- 查出本週清單和週五／週日結算會重新整理當日的實際卡片。使用者還在輸入頁碼時，中途數字可能與其他範圍合併，改變卡片識別，畫面上的後續操作因此找不到原項目。
- 現在本週清單及結算只讀取當日紀錄的深層副本，不重建正在編輯的卡片，也不重套 Calendar 預設值。其他日期仍載入並整理排程後統計；本週清單勾選當日項目時也保留畫面所對應的卡片。
- 化學「123日的淬鍊」109–114 頁正常顯示 `Chapter 3 物質間的反應（p.109–114）`。教材對照表本身沒有改動。
- 「訂正」正常展開「錯因／不熟觀念」；取消再勾選不清空內容。單張卡、Calendar 合併子卡及延期子卡均納入測試。
- 原本 Calendar 明確頁碼優先、重新同步與載入時合併、延期、計時、完成率公式等規則保持不變。

## 套用方式

- 主專案已更新；`gsat-study-tracker-v171.0.83-update` 僅包含本次變更檔案，不製作 ZIP。
- 把更新資料夾內的內容依原路徑合併覆蓋到已套用 v171.0.82 的完整專案，再重新部署前端。更新資料夾不能單獨執行。
- 不需重新部署資料庫或 Edge Functions，不需修改 Secrets、重新授權或刪除任何紀錄。
- 前端部署後重新整理 Tracker。若先前操作已失效而未存入，請重新填入正確頁碼、勾選訂正，再儲存；本次修正不會猜測或重建未保存的筆記。

## 驗證

- 全部 196 項測試通過，包含新增 5 項回歸測試；TypeScript 檢查與正式建置通過。
- 測試亦重現舊版錯誤：同日有生物及化學卡時，把結束頁逐字輸入到「1」，統計重整會讓原化學卡識別失效，後續頁碼更新及訂正勾選均被忽略；修正後同一操作可正常完成。
- 新增測試涵蓋化學頁碼邊界、週二／週五／週日統計不更動當日資料、逐字輸入、Calendar／延期子卡、錯因顯示與獨立保存、取消再勾選及重新載入。
- 本機瀏覽器實測：逐字輸入 109–114 → Chapter 3 即時更新 → 勾選訂正 → 填錯因 → 儲存 → 重新整理後內容保留；沒有偵測到瀏覽器錯誤。
- 未操作正式網站的輸入或儲存，未變更雲端紀錄；尚未部署至正式環境。

## Commit 建議

`fix(study): keep summary rendering from rebuilding active cards`
