# 最新更新

版本：v171.0.82

## 本次更新

- 已直接讀取 Google Calendar 核對：2026/9/2 生物「光合作用」備註指定 p.16–18；同日另有 8/24 延後的化學「原子結構、同位素與週期表」，指定 p.45–62。
- 根因：自然科建議原本以「日期」共用一筆資料，化學行程覆蓋生物。解析器有正確讀出 p.16–18，問題發生在轉成卡片時，不是使用者排程或反引號造成。
- 修正為依每張卡自己的 Calendar 行程識別取值；不同科目、同科不同範圍、當日與 Calendar 重排項目均獨立處理，讀取順序不再影響結果。
- 所有自然科卡片與子卡各自更新建議頁數及歷史完成覆蓋，不再只更新當日第一張自然卡。
- 明確指定的 Calendar 頁碼優先；缺頁碼時僅採用相同科目、相同教材的單元預設，不拿其他教材的頁碼代替。
- 重新同步會重新整理既有卡片；來源日期先更新，再整理延期目標日。已繼承錯誤頁碼的自然科延期卡也會更新，保留分鐘、筆記與明確的使用者頁碼修改。
- v171.0.81 的延期完成率和左下角刪除按鈕規則不變。

## 部署與使用

- 主專案已直接更新；另外提供 `gsat-study-tracker-v171.0.82-update`，只含本次變更檔案，保留原目錄結構，不製作 ZIP。
- 更新資料夾需合併覆蓋至已套用 v171.0.81 的完整專案，不能單獨執行。
- 只需重新部署前端；不需重新部署資料庫、Secrets 或 Edge Functions。
- 部署完成後重新整理 Tracker → 展開「連線設定」→ Google Calendar「立即同步」→ 查看 2026/9/2。
- 不需清除紀錄、刪除 Calendar 行程或重新授權。明確手動修改過的頁碼仍優先保留。

## 驗證

- 新增 10 項回歸測試，包含 9/2 實際備註、正反讀取順序、既有錯誤值修復、同科不同範圍子卡、獨立完成判斷、手動覆寫、單元 fallback、延期與再次延期。
- 全部 191 項測試通過；TypeScript 檢查與正式建置通過。
- 測試執行實際 Calendar → 卡片定義 → 紀錄重新整理函式，不只驗證頁碼解析器；無連線寫入與正式帳號操作。
- 未更動正式網站或雲端紀錄；尚未部署至正式環境。

## Commit 建議

`fix(calendar): isolate natural-science page ranges per event`
