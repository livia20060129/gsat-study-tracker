# 最新更新

版本：v171.0.76

## 本次有更新的項目

- 修正按下「完成並填入」後，分鐘欄位看似沒有填入的問題。
- 完成計時後會自動切回「手動」，讓寫入的分鐘數立即顯示在輸入框中。
- 本次計時結果會直接取代舊的手填時間，不會被舊值覆蓋回去。
- 已開始的極短計時即使不足 1 秒，也會填入至少 1 分鐘；尚未開始計時則不會憑空填入。
- 主卡片、合併卡片與子卡片皆共用相同寫入流程。
- 新增 2 項完成計時回歸測試，完整測試目前共 155 項。

## 部署範圍

- 僅需重新部署前端；不需更新 Supabase Database 或 Edge Functions。

## 更新資料夾

- `gsat-study-tracker-v171.0.76-update`
- 僅包含本次有修改的檔案，並保留原本目錄結構；未建立 ZIP。

## Commit 建議

`fix(timer): show and persist completed timer minutes`
