# 最新更新

版本：v171.4.2

## 科目圓環明細

- 點擊科目後，圓環會由原位置平順移向左側，再顯示右側項目明細。
- 明細小圓環改用較細的環，不影響全部科目畫面的原始粗細。
- 右側每個項目改為兩列：第一列顯示較大的項目名稱，第二列顯示百分比與小時數。
- 項目名稱不再重複顯示目前科目；講義類項目改用「講義版本＋冊別＋進度／題目」區分。
- 科目圓環中央與右側明細統一使用 `hr`，四捨五入並固定顯示到小數點後一位。

## 日期圓形圖層修正

- 日期圓心不再使用透明色疊在外圈上，而是直接依學習時數算出不透明的藍色。
- 完成率只控制獨立的綠色外圈；100% 完成只把外圈填滿，不會改變或透入圓心。

## 返回按鍵

- 「回到 Tracker」移到週／月滑塊上方。
- 改為與主頁主要操作同等明顯的藍色按鍵，桌面與手機皆維持清楚可點擊。

## 原有學習總結

- 週曆／月曆成為頁面主要視覺，日期圓內深淺代表完成學習時間，外圍圓環代表完成率，100% 使用深綠完整圓環。
- 固定畫面不顯示每日分鐘與百分比；桌面指向或手機點擊日期時才顯示日期、學習時間與完成率。
- 科目圓環沿用首頁粗細，科目單字直接放在圓環上，總小時數放在中央，右側不重複列出科目。
- 點擊科目後切換成較小的同色系項目圓環，依項目占比決定深淺，右側顯示項目顏色、比例與小時數。
- 完成時間排除已確認延期的原項目，並依穩定來源識別去除重複 Calendar／延期紀錄。

## 固定小結

- 不呼叫任何 AI API，也不產生自由文字。
- 學習時數變化以相對 5% 為門檻；完成率以 5 個百分點為門檻，固定分類為增加、持平或下降。
- 週模式標示「本週小結」，月模式標示「本月小結」；相同資料一定得到相同評語。

## 首頁計時

- 從手動切換至計時時，會把既有分鐘數換算成最接近的秒數作為計時起點；空白或無效值從 00:00 開始。
- 例如手動填入 `12.5` 分，切換後計時器由 `12:30` 接續，不會遺失已記錄時間。

更新資料夾：`gsat-study-tracker-v171.4.2-calendar-layer-separation`

## 驗證

- 329 項單元／回歸測試通過。
- TypeScript 型別檢查與 Vite 正式建置通過。
- 科目圓環名稱、講義版本分組與新版明細結構均有回歸測試保護。

本次不需要新增 Supabase migration，也不需要重新部署 Edge Function；只需部署新版 GitHub Pages 前端。

## Commit 建議

`fix(summary): separate calendar layers and standardize hour display`
