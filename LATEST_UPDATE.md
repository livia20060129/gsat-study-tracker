# 最新更新

版本：v171.0.72

## 本次有更新的項目

- 合併卡片中的每張子卡片現在都有自己的「手動／計時」切換按鈕。
- 手動模式可分別填入每張子卡片的完成分鐘數。
- 計時模式可分別開始、暫停、繼續、完成填入與重設。
- 同一時間仍只允許一個項目計時；切換到另一張子卡片時會先詢問是否暫停原項目。
- 子卡片的分鐘數與計時進度會回寫至各自的來源項目，重新整理、Calendar 更新與雲端同步後仍會分別保留。
- 合併大卡片不再顯示一個容易混淆的共用時間欄。
- 新增子卡片獨立分鐘數與計時狀態的回歸測試。

## 部署範圍

- 僅需重新部署前端；不需更新 Supabase Database 或 Edge Functions。

## Commit 建議

`feat(timer): add independent time controls to grouped child cards`
